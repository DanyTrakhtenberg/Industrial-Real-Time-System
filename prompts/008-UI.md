Implement React TypeScript UI with exactly 3 pages.

Pages:
1. Dashboard
   - live cards for 20 sensors
   - SignalR real-time updates
2. Sensors
   - table of sensors from GET /api/sensors
3. History
   - select sensor
   - show history from GET /api/sensors/{id}/history

Do not create extra pages.
Use React Router.